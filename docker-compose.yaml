services:
  app:
    image: spaiss5:8
    container_name: python_app
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    working_dir: /app
    environment:
      - MODEL_DIR=/models
    volumes:
      - ./main.py:/app/main.py
      - ./models:/models
    ports:
      - "8000:8000"
