# --- Web serving ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-multipart>=0.0.12

# --- Model loading / inference ---
safetensors>=0.4.5
transformers>=4.46
accelerate>=1.1
huggingface-hub>=0.26
hf-transfer>=0.1.8          # fast parallel weight downloads
numpy>=2.0

# torch is installed separately in the Dockerfile from the CUDA-matched index.
